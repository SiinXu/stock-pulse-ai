Repository: SiinXu/stock-pulse-ai. You are in an isolated Orca worktree branched from origin/main.
Run `unset GH_TOKEN` first if `gh` returns 403 (env token is read-only; keyring token has repo scope).

## Facts (sampled 2026-08-27 after #1538 merged; re-verify at HEAD before acting)
- `src/data_provider/base.py` is 2382 lines / 94 KB, down from 3370 across #1537 and #1538.
- `src/data_provider/manager_parts/` contains 13 modules. The established pattern is:
  a `_<Name>Methods` class in the part module, an `EXPECTED_*_METHOD_NAMES` tuple, a
  `bind_*_facade` function, an `_install_facade_reload_hook`, and a matching
  `_assemble_*_facade` in base.py's tail wired into `_assemble_data_fetcher_manager_facades`.
  Read `manager_parts/fundamental_context_methods.py` and base.py's `_assemble_*` tail first —
  copy that shape exactly.
- Slices 1-12 are already documented in `docs/data-provider-ownership.md`. Yours is slice 13.

## Your task — ONE behaviour-preserving PR, `Refs #1067` `Refs #1088`
Extract the manager-side rankings cluster out of `DataFetcherManager` into a new
`src/data_provider/manager_parts/rankings_methods.py`.

The cluster is CONTIGUOUS in base.py (~line 1875 to the end of the class, ~510 lines):
  _get_sector_rankings_with_meta, get_sector_rankings, _copy_ranking_rows,
  clear_concept_rankings_cache_for_tests, get_concept_rankings, get_hot_stocks,
  get_limit_up_pool
Re-derive that list and those line numbers yourself at HEAD — do not trust these numbers blindly.

CRITICAL BOUNDARY — do NOT move these:
  `BaseFetcher.get_main_indices`, `BaseFetcher.get_market_stats`,
  `BaseFetcher.get_sector_rankings`, `BaseFetcher.get_concept_rankings`,
  `BaseFetcher.get_hot_stocks`, `BaseFetcher.get_limit_up_pool` (around base.py lines 239-303).
  Those are abstract-provider methods on a DIFFERENT class that share names with the manager
  methods. Moving them would break the provider contract. Only `DataFetcherManager` members move.
  Also leave `DataFetcherManager.get_main_indices` (~1496) and `.get_market_stats` (~1539) in
  place — they are not contiguous with your block and are a separate future slice.

`clear_concept_rankings_cache_for_tests` is a `@classmethod` and `_copy_ranking_rows` is a
`@staticmethod`; preserve those decorators and their binding semantics exactly.

If after reading you judge a smaller sub-cut is materially cleaner, take the smaller cut and say
why. A smaller landed PR beats a bigger blocked one.

## Method (ADR-006 / docs/service-split-template.md — read both first)
- Mechanical move + re-export ONLY. ZERO behaviour change in this PR.
- Keep the public `DataFetcherManager` API and every existing import path working via the
  existing facade-assembly mechanism in base.py's tail.
- Do NOT change fallback ordering, circuit/health semantics, cache TTL, or cache-key computation.
  If you notice a bug while moving, write it in your report and KEEP THE BEHAVIOUR.
- Do NOT introduce a bare `get_config()` call site in the new module — `check_config_access.py`
  bans that for new modules and it will fail CI. If the moved code needs config, reach it through
  an accessor left on the facade (this is exactly what #1538 had to do; see issue #1540).

## Verification (paste actual output in the PR body)
  python scripts/check_config_access.py
  python scripts/check_hot_path_module_size.py
  python scripts/check_import_layers.py
  python scripts/check_layer_direction.py
  python -m pytest tests/data_provider/ -q -k "ranking or sector or concept or hot_stock or limit_up"
Then regenerate `scripts/hot_path_module_size_baseline.json` via `--write-baseline`; the diff must
show base.py SHRINKING. A baseline that grows is a failure — stop and report. If `--write-baseline`
refuses because your own earlier value is now smaller than reality, restore the file from
origin/main first, then regenerate.

Also add a facade test modelled on `tests/data_provider/test_fundamental_context_methods_facade.py`.

STRONG verification you are expected to run and report: AST-compare each moved function body
against its definition on origin/main and report "N identical / M different / K missing".
Anything other than all-identical needs an explicit explanation.

## Repo rules (hard)
- Changelog: create `docs/changelog.d/<slug>.md` ONLY; NEVER edit `docs/CHANGELOG.md`.
- `Refs #1067` and `Refs #1088`. NEVER `Closes`/`Fixes`/`Resolves` (4 silent-issue-close incidents).
- English commit messages and PR body. No `Co-Authored-By`.
- No test deletion/weakening, no skip/xfail, no mocking away the real risk layer.
- Touch ONLY `src/data_provider/base.py`, `src/data_provider/manager_parts/`,
  `docs/data-provider-ownership.md`, the baseline, and tests. Do not touch `apps/dsa-web`.
- Update `docs/data-provider-ownership.md` as slice 13, matching the existing slice prose shape.
- Open the PR. Do NOT merge it.
- PR title: `refactor: extract DataFetcherManager rankings from base.py`

## PR body MUST contain a `## Self-review` section with SEVEN numbered lines
1. Unreachable code  2. Mocked-away risk layer  3. Fail-open paths  4. Boundaries (empty/oversized/
concurrent/timezone/first-run/unconfigured)  5. Contract drift vs existing callers  6. Unregistered
config keys  7. PR-body-vs-diff mismatch.
For each, write the finding or literally `none found — checked <what you checked>`. Fix what you find,
then re-review. Converged only after TWO consecutive clean rounds; if THREE rounds don't converge,
STOP and report instead of stacking patches.

## Premise check (mandatory)
If the evidence contradicts this brief — the cluster isn't contiguous, the counts differ, the
BaseFetcher/manager split is not as described — THE EVIDENCE WINS. Report with file/line and adjust
or stop. Blind execution of a wrong brief is the worst outcome here.

## Exit protocol
Push your FIRST meaningful commit immediately (`git push origin HEAD:<your branch>`) and push after
every subsequent step. Unpushed local work is treated as lost.

## Report back
Evidence per claim (command + output excerpt), base.py line count before/after, the AST comparison
result, the baseline diff, every deviation from this brief with its reason, everything you refused
to do and why, and the PR number.
