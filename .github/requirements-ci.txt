# Backend CI dependencies.
-c ../constraints.txt
-r ../requirements.txt

# Bootstrap, test, and lint tooling. Versions resolve through constraints.txt.
pip
flake8>=7,<8
pip-audit
pytest>=9.0.3,<10
uv==0.11.31
