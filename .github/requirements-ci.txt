# Backend CI dependencies.
-r ../requirements.txt

# Test and lint tooling used by backend-gate.
# Bounded to the majors CI currently resolves so tool releases cannot
# break CI without a code change.
flake8>=7,<8
pytest>=8,<9
