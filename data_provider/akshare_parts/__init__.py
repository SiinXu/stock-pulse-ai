# -*- coding: utf-8 -*-
"""Internal implementation parts for the AkShare data-provider fetcher.

Modules under this package are private to ``data_provider``. External callers
must continue to import from ``data_provider.akshare_fetcher`` (or
``data_provider``) so public surfaces and test patch targets stay stable.
"""
