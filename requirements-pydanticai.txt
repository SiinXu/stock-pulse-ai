# Optional dependency for the experimental PydanticAI runtime adapter (AR-PY-04).
#
# NOT part of the default install: Native is the permanent default runtime and
# has zero dependency on this file (see docs/architecture/ADR-001-agent-runtime.md).
# Install only to exercise the opt-in PydanticAI POC path:
#
#   pip install -r requirements-pydanticai.txt
#
# 方案 B uses a custom PydanticAI Model that reuses StockPulse's in-process
# LLMToolAdapter, so only the slim core is needed — this deliberately avoids the
# provider model extras (openai/anthropic/google), which would pull tiktoken>=0.12
# and conflict with StockPulse's tiktoken<0.12 pin (issue #537).
pydantic-ai-slim==2.12.0
