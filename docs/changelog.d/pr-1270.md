- [Changed] Historical Decision Reflection injection revalidates every renderer, admits only bounded finite structured outcomes with `signal_id` provenance, wraps the analysis prompt block as untrusted memory data, and keeps free-form signal reason text out of the inject path (Fixes #118; aligns with #1119 admission and #1017 isolation).
