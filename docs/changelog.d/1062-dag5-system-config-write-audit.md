- [Added] Emit fail-closed `system_config.write` security-audit events from `SystemConfigService.update` for HTTP config save, config-profile apply/import, onboarding apply, local-model config writes, and legacy channel migration, with attempt-before-persist and additive 503 `operation_completed` semantics. HTTP PUT `/config` no longer double-emits; env import/export/rollback keep their existing event types (Refs #1062).
- [Tests] Cover service-level write audit happy paths, attempt fail-closed, persist-vs-block 503, preserved domain 400/409, bounded metadata, durable query, redaction, HTTP single-emit, import nested non-emission, empty profile/onboarding no-ops, and local-model persist (Refs #1062).
- [Docs] Mark SystemConfigService.update writes as Landed on the privileged coverage map; keep issue #1062 open for remaining named later-owner and deferred rows (Refs #1062).
