- [Added] Emit fail-closed `system_config.write` security-audit events from `SystemConfigService.update` for HTTP config save, config-profile apply/import, onboarding apply, local-model config writes, watchlist `STOCK_LIST`, and legacy channel migration, with attempt-before-persist and additive 503 `operation_completed` plus `config_version` / `applied_count` / `reload_triggered` field parity. HTTP PUT `/config` no longer double-emits; env import/export/rollback keep their existing event types (Refs #1062).
- [Tests] Cover service-level write audit happy paths, attempt fail-closed, persist-vs-block 503, preserved domain 400/409, watchlist Landed 503 honesty, bounded metadata, durable query, redaction, HTTP single-emit, import nested non-emission, empty profile/onboarding no-ops, local-model persist, and auth-cache isolation after config canaries (Refs #1062).
- [Docs] Mark SystemConfigService.update writes, including watchlist STOCK_LIST, as Landed on the privileged coverage map; keep issue #1062 open for remaining named later-owner and deferred rows (Refs #1062).
