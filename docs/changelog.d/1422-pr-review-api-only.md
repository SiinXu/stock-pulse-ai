- [Changed] Opt-in PR Review now requires a positive integer `pr_number` and inventories changed files with a SHA-pinned GitHub compare of captured head/base SHAs without checking out the PR head (Refs #1422).
- [Fixed] PR Review `security-check` binds file inventory to SHA-pinned `repos.compareCommits` of captured head/base SHAs so a B→A→B force-push cannot desynchronize inventory from downstream checkout SHAs; `pulls.listFiles` cannot pin a SHA, a re-get equality guard alone does not close that ABA race, and fork inventory uses the same SHA...SHA compare rather than `USERNAME:BRANCH` / `owner:SHA` (Refs #1422).
