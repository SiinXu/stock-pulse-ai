- [Fixed] Layer-direction and import-cycle ratchets now measure import-time imports through a shared whole-file AST visitor, so reverse imports nested in module-level `try`/`except`/`else`/`finally`, `if`/`else`, `with`/`async with`, loop bodies and their `else`, `match` cases, and class bodies are counted instead of silently dropped. Imports inside `def`/`async def` bodies stay excluded, and the `if TYPE_CHECKING:` exclusion is now binding-aware across `TYPE_CHECKING`, aliased imports, and `typing`/`typing_extensions` module attributes (Refs #1555).
- [Added] `scripts/layer_direction_baseline.json` gains an advisory `lazy_exceptions` inventory recording the function-local reverse imports that are not also currently scanned import-time reverse edges, with drift notes printed by the guard. Drift is never enforced, never counted against `hard_ceiling`, and never changes the exit code; a malformed `lazy_exceptions` section is still rejected through the guard's standard `ERROR: invalid-baseline:` line (Refs #1555).
- [Tests] Cover import placement across eager, function-local, and `TYPE_CHECKING` forms in both guards' `--self-test` and pytest suites, plus lazy-inventory dedupe, drift, refresh, CLI rejection of every malformed lazy section shape, `--write-baseline` repair of one, a pin that advisory drift keeps the guard at exit `0` in both directions, and shrink-only (`<=` / subset) pins that the shipped 12-edge and 11-pair inventories did not grow — including fixture counterexamples that the named repository tests plus the CLI stay green after a legitimate shrink and fail on growth (Refs #1555).
- [Docs] Correct the "module body imports only" wording in `docs/layer-direction-ratchet.md`, `docs/import-cycle-ratchet.md`, `docs/testing-ci-gate.md`, ADR-010, both guard docstrings, and both baseline descriptions to the actual import-time scope, and document the advisory lazy inventory (Refs #1555).
