- [Chore] Extract DataFetcherManager CN fundamental sub-blocks (`get_capital_flow_context`, `get_dragon_tiger_context`, `get_board_context`) into `src/data_provider/manager_parts/fundamental_cn_context_methods.py` behind the ADR-006 facade, converting those three `get_config()` sites to `self._get_fundamental_config()` without changing fallback, status, source-chain, or retry semantics (Refs #1067).
