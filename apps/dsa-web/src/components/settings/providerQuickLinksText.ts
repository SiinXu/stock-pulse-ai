import type { UiLanguage } from '../../i18n/uiText';

export const PROVIDER_QUICK_LINKS_TEXT: Record<UiLanguage, { opensInNewTab: string }> = {
  zh: { opensInNewTab: '将在新标签页打开' },
  en: { opensInNewTab: 'opens in a new tab' },
};
