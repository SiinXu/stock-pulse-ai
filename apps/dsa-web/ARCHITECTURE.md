# StockPulse Web Architecture Contract

This document defines ownership and dependency direction inside `apps/dsa-web/src`. It complements
the visual and interaction contracts in `DESIGN_GUIDE.md` and `docs/web-ui-foundation.md`; it does
not replace them. Runtime behavior, API contracts, accessibility, and repository `AGENTS.md` rules
remain authoritative.

## Goals

- Keep business and data contracts reusable without importing rendered UI.
- Keep shared UI primitives independent of feature, layout, and theme owners.
- Make page modules composition roots rather than reusable dependencies.
- Keep the component playground isolated behind its application-level route composition.
- Turn known debt into explicit, shrinking exceptions instead of implicit precedent.

The contract governs production TypeScript and TSX. Tests may cross production boundaries to
exercise integration paths, and generated, fixture, story, and test sources are excluded from the
production inventory.

## Ownership Layers

| Layer | Paths | Owns |
| --- | --- | --- |
| Application composition | `App.tsx`, `main.tsx` | Providers, router assembly, and top-level error/loading boundaries. |
| Pages | `pages/` | Route-level orchestration, business-state composition, and page-local presentation. |
| Feature UI | `components/<feature>/` | Reusable presentation and interaction for one feature domain. |
| Shared UI | `components/common/`, `components/layout/`, `components/routing/`, `components/theme/` | Design primitives and cross-feature visual or navigation patterns, each under its named owner. |
| Behavior and state | `hooks/`, `stores/`, `contexts/` | Reusable React behavior, client state, and neutral provider contracts. |
| Data and contracts | `api/`, `types/`, `utils/`, `i18n/`, `locales/` | Transport adapters, schemas, pure policy/helpers, and localization resources. |
| Component catalog | `playground/` | Component scenarios and manual inspection surfaces mounted only by application composition. |

`dev/` contains optional development integration shims. Assets contain static resources and do not
define application contracts.

## Dependency Direction

The normal composition direction is:

```text
App -> pages -> feature/shared UI -> behavior/state -> data/contracts
```

This is an ownership rule, not a requirement to pass every dependency through every layer.
Pages and components may import the lower-level modules they directly need. The following rules are
enforced by `src/components/__tests__/architectureImportGuard.test.ts`:

1. `App.tsx` owns page-route composition, and `main.tsx` may import `App.tsx` to mount the
   application. Pages, components, and lower layers must not import a page or either application
   composition module. Extract reusable UI or policy below the route layer instead.
2. A page must not import another page. Extract reusable UI to a feature component, and extract
   reusable behavior or policy to its owning lower layer.
3. Pages, components, and lower layers must not import the playground. `App.tsx` is the sole
   composition root allowed to mount its catalog routes.
4. `components/common/` must not import or re-export another UI owner's feature, layout, routing,
   or theme module. Consumers import those modules from their actual owner.
5. `api/`, `contexts/`, `hooks/`, `i18n/`, `locales/`, `stores/`, `types/`, and `utils/` must not
   import rendered UI under `components/`, route composition under `pages/`, or playground code.
   Move a shared contract to neutral ownership instead of making a lower layer depend on its UI.

Feature components may compose shared UI and lower layers. Layout, routing, and theme owners may
compose `components/common` primitives. Application composition may import pages and any provider
needed to assemble the application.

### Portfolio route-local workflows

`hooks/portfolio/` contains feature-private workflow Modules for the Portfolio route. They live
under the behavior/state layer but are not a shared cross-feature facade:

- `usePortfolioProjectionSession` owns account/cost scope acceptance, stale-response rejection,
  snapshot-to-risk projection (TanStack Query schedule), hand-rolled ledger query dispatch, and the
  refresh surfaces used after writes.
- `usePortfolioLedgerMutationWorkflow` owns operation identity for the five idempotent ledger
  writes, commit-before-refresh sequencing, paper-trade refresh-only retry, and CSV partial-result
  attempt rotation.

`PortfolioPage` remains the route composition root and presentation owner: it owns URL composition,
form validation, modal state, localized error presentation, tables, and all rendered structure.
The route-local workflows consume the existing Portfolio transport adapter and public Web types;
they do not redefine either contract.

## Module Entry Points

- A directory barrel may expose modules owned by that directory. It must not hide an upward or
  sideways dependency by re-exporting another owner's module.
- Prefer the narrowest stable owner import. Store consumers should import the owning store module;
  UI consumers may use a same-owner component barrel when it represents a deliberate public API.
- Do not add a new barrel solely to shorten import paths. A barrel is useful only when it defines a
  reviewed ownership boundary.
- Types and pure policy used by both UI and non-view code belong in `types/`, `utils/`, or a neutral
  context module, not in a component directory.

## Executable Inventory And Ratchets

`src/components/__tests__/productionSourceInventory.ts` is the shared test-only authority for
discovering production TypeScript, TSX, and CSS. Architecture, page/router, production-design, and
responsive-design guards use that inventory so exclusions cannot drift between tests.

The architecture guard parses static imports, re-exports, import types, dynamic imports, and
`require()` calls with the TypeScript AST. It resolves relative imports to the production inventory
and compares every violation with an exact allowance ledger. Each allowance records its owner and
removal condition. The current maximum is one; a new edge fails even if it resembles existing
debt.

The production design guard separately caps existing caller-specific exceptions:

| Allowance | Maximum |
| --- | ---: |
| Button visual overrides | 0 |
| State-surface visual overrides | 16 |
| Near-viewport panels | 1 |

These numbers are ceilings, not targets. When a violation is removed, delete its exact allowance
and lower the corresponding maximum in the same change. Do not replace one retired exception with
another or broaden an exact path/token entry into a directory-wide exclusion.

## Known Migration Ledger

The executable allowance list is authoritative. The current ledger contains these migration
groups:

| Current edge | Count | Removal path |
| --- | ---: | --- |
| `hooks/useSystemConfig.ts` imports Settings subcategory policy | 1 | Extract the cohesive policy in a dedicated Settings contract change. |

W2a introduced the guard with eight allowances. W2b resolved four by removing the foreign
layout/theme re-exports from `components/common/index.ts`; it also removed the unused stores facade
and analysis store. W2c resolved three more by moving the route-focus context and model-access
field-key contracts to neutral ownership. Runtime consumers now import these contracts from their
actual owners, leaving one deferred exception.

The Settings exception is intentionally deferred. It spans configuration schema grouping, route
selection, page navigation, and tests; moving it safely requires a focused behavioral slice rather
than a line-count-driven file split.

## TanStack Query Rollout Pattern (Pilot)

`@tanstack/react-query` is mounted once at the application root via `query/QueryProvider.tsx` in
`main.tsx`. The provider is **inert for non-consumers**: pages and hooks that never call
`useQuery` / `useMutation` keep their existing fetch style and are unaffected.

### Pilot consumer

| Surface | Ownership | Behavior parity notes |
| --- | --- | --- |
| `MarketReviewPage` history list | `hooks/useMarketReviewHistoryQuery.ts` | `useQuery` with `refetchInterval: 30_000`, `refetchOnWindowFocus: true`, `retry: false`. First fetch uses the store's non-silent load; later refetches use silent refresh. Errors still land on the existing store / `ApiErrorAlert` surfaces. |
| Market review trigger | `hooks/useMarketReviewRunner.ts` | `useMutation` for `triggerMarketReview`. Task-status polling stays custom (domain notices, 2s cadence, 120-attempt cap) so completion/timeout copy is unchanged. |

### Migration ledger (graduated pages)

| Surface | Ownership | Behavior parity notes | Status |
| --- | --- | --- | --- |
| `MarketReviewPage` history + trigger | `hooks/useMarketReviewHistoryQuery.ts`, `hooks/useMarketReviewRunner.ts` | See pilot consumer above | Pilot (#788) |
| `DecisionSignalsPage` list feed | `hooks/useDecisionSignalListQuery.ts` | No interval poll; no focus refetch (`refetchOnWindowFocus: false`); key includes filters/page/scope/watchlist readiness; list reducer remains error/loading owner; `retry: false` | Wave 1 |
| `DecisionSignalsPage` outcome stats | `hooks/useDecisionSignalOutcomeStatsQuery.ts` | Mount load only; no focus/poll; existing stats error surface; `retry: false` | Wave 1 |
| `DecisionSignalsPage` detail outcomes + feedback | `hooks/useDecisionSignalDetailQueries.ts` | Selection-gated; independent queries; no focus/poll; `retry: false` | Wave 1 |
| `DecisionSignalsPage` status mutation | `hooks/useDecisionSignalStatusMutation.ts` | Single-shot `useMutation`; synchronous in-flight ref (not `isPending` alone) stays held through page-owned list+stats reload; `retry: false`; latest/timeline/selection updates stay page-owned | #789 |
| Alerts rules / triggers / notifications | `hooks/useAlertWorkspaceQueries.ts` + `AlertsWorkspace` | No poll; no focus refetch; page-owned create/update/delete mutations; `retry: false` | Wave 1 |
| `SkillOutcomesPage` performance load | `hooks/useSkillOutcomesQuery.ts` | reloadToken-driven initial load; manual refresh stays page-owned; no focus/poll; `retry: false` | Wave 1 |
| `ApprovalsPage` workspace | `hooks/useApprovalsWorkspaceQuery.ts` | First run full load; later ticks poll proposals at **5s**; interval off when auth-blocked; no focus refetch; countdown timer stays local | Wave 1 |
| `StockDetailsPage` quote + history | `hooks/useStockDetailsQueries.ts` | Code/days key; no poll/focus; `retry: false` | Wave 1 |
| Analysis Workbench dashboard data refresh | `hooks/useDashboardDataRefreshQuery.ts` (via `useDashboardLifecycle`) | First run **per mount** (mount-scoped query key + cache miss) non-silent history + stock-bar + active tasks; initial-loader identity change re-runs non-silent path; later ticks silent at **30s**; explicit `visibilitychange` refetch (`refetchOnWindowFocus: false`); unmount removes schedule cache entry; SSE + 2s disconnected task poll stay custom; `retry: false` | Wave 2 (#789) |
| `HomePage` attention / Today's Focus / setup-status | `hooks/useHomePageQueries.ts` | Mount + manual refresh only; no poll; no window-focus refetch; Today's Focus key includes language; attention pack uses allSettled so one failed source does not wipe the pack; last-known signal totals stay marked stale; setup silent refresh stays onboarding-owned; unmount removes cache rows; `retry: false`; `staleTime: 0` | Wave 2 (#789) |
| Header notification bell preview + unread count | `hooks/useUnreadNotifications.ts` | **60s** poll (`pollMs` default 60000, `pageSize` 10, `enabled` true); query key includes `pageSize` only; `list({ pageSize })` + `unreadCount()` via `Promise.allSettled`; last-good per side from live cache after settlement; independent flags; hard error only when both fail; bounded `sourceStatuses` degradation; `retry: false`; `refetchOnWindowFocus: false`; `refetchIntervalInBackground: true`; `networkMode: 'always'` (previous effect always fetched while offline); `staleTime: 0` + unmount/disable/key-change silent cancel + `removeQueries` (no hidden remount or disable-period cache; not `gcTime: 0`); `refresh` void-facing (silent cancel then `refetchQueries` so an initial pending pair is replaced); `markAllSeen` keeps `markAllRead` success/failure/rethrow and, while disabled, updates only `markFailed` so it cannot resurrect the removed row; the disabled shape reports the empty preview with live `markFailed`. Two disclosed divergences from the previous `setInterval` scheduler, both unreachable from the sole consumer: a tick during an in-flight pair joins that fetch instead of starting a second pair, and a runtime `pollMs` change re-arms the interval without an immediate refetch. Cleanup is `removeQueries({ exact: true })`, correct for a **single owner** only — `Shell` mounts the bell on mutually exclusive desktop/mobile branches; a second concurrent owner of this key would need a refcounted discard. Notification Center page inbox is migrated on a disjoint `['notifications','center','list',…]` family (see below); this hook must not prefix-cancel or prefix-remove `['notifications']`. | #789 |
| Portfolio projection snapshot + risk | `hooks/portfolio/usePortfolioProjectionQueries.ts` + `usePortfolioProjectionSession` | Mount + filter-driven / refresh / silent FX follow-up snapshot-then-risk; `retry: false`; no poll; `refetchOnWindowFocus: false`; `staleTime: 0`; snapshot fail clears snapshot+risk and the page hard-error surface; silent `CancelledError` does not clear; risk fail keeps snapshot, sets `riskWarning`, and clears hard error; owner-based `isLoading`; exact-key cancel + `removeQueries` on unmount/account scope. Ledger events stay hand-rolled in the same session hook. | #789 |
| Notification Center page inbox list, cursor pagination, and mark-read refresh | `hooks/useNotificationCenterInbox.ts` | Imperative `fetchQuery` only (no live `useQuery` observer, no `useInfiniteQuery`, no `useMutation`); query key `['notifications', 'center', 'list', kind\|\|'all', unreadOnly ? 'unread' : 'all', 50, cursor ?? 'head']`; `retry: false`; no poll; `refetchOnWindowFocus: false`; `networkMode: 'always'`; `staleTime: 0`; request-id owner loading/error; concatenated `items` and latest `pageData` stay in React state (cache is transport-only); exact-key silent cancel (`silent: true, revert: false`) + `removeQueries`; same-key refresh cancel+remove then `fetchQuery`; unmount/filter change exact-removes live center keys; mark-read / mark-all-read stay imperative `notificationInboxApi` plus generation-fenced `load('refresh')` (no optimistic patch, no bell `setQueryData` / `invalidateQueries`); header bell `['notifications','unread-preview',10]` stays disjoint | #789 |
| Settings system-config GET load | `hooks/useSystemConfigLoadQuery.ts` + `useSystemConfig` | Imperative `fetchQuery` only (no live `useQuery` observer, no `useMutation`); query key `['settings','system-config','load']`; always `getConfig(true)`; `retry: false`; no poll; `refetchOnWindowFocus: false`; `networkMode: 'always'`; `staleTime: 0`; request-id + `serverSnapshotEpoch` latest-wins; silent `CancelledError` does not `setLoadError`; exact-key silent cancel (`silent: true, revert: false`) + `removeQueries`; overlapping `load()` cancel+remove then `fetchQuery`; unmount exact-removes the load key; save / 409 rebase / `refreshCommittedSnapshot` / `refreshAfterExternalSave` stay raw `systemConfigApi.getConfig(true)` (must not share the load key); section-local Settings loaders stay hand-rolled | #789 |
| Event Calendar page list | `hooks/useEventCalendarQuery.ts` + `EventCalendarWorkspace` | Imperative `fetchQuery` only (no live `useQuery` observer, no `useInfiniteQuery`, no `useMutation`); query key `['event-calendar','list',dateFrom,dateTo]`; transport `eventCalendarApi.getCalendar({dateFrom,dateTo},{signal})`; `retry: false`; no poll; `refetchOnWindowFocus: false`; `networkMode: 'always'`; `staleTime: 0`; request-id owner loading/error; React state for `data` (cache is transport-only); exact-key silent cancel (`silent: true, revert: false`) + `removeQueries`; date-range change and Refresh share `load()`; same-key refresh cancel+remove then `fetchQuery`; unmount/date-key change exact-removes live keys; silent `CancelledError` / abort does not `setError` or clear a newer generation's data; live-generation hard error clears only its own data; `includeImpact` stays client-side column state (not in key or API) | #789 |
| Token Usage dashboard GET | `hooks/useTokenUsageQuery.ts` + `TokenUsagePage` | Imperative `fetchQuery` only (no live `useQuery` observer, no `useMutation`); query key `['usage','dashboard', period]`; transport `usageApi.getDashboard({period, limit: 50})`; `retry: false`; no poll; `refetchOnWindowFocus: false` (overrides app-root default `true`); `networkMode: 'always'`; `staleTime: 0`; request-id owner loading/error; React state for `{period, dashboard}` (cache is transport-only); displayed dashboard only when snapshot period matches URL period; exact-key silent cancel (`silent: true, revert: false`) + `removeQueries`; period change and Refresh/Retry share `load()`; same-key refresh cancel+remove then `fetchQuery`; unmount/period-key change exact-removes live keys; same-period refresh fail keeps last-good snapshot; other-period snapshot is hidden; silent `CancelledError` does not `setError`; newest generation wins. SettingsPage loads `TokenUsagePage` through `React.lazy` so the hook is not billed to the SettingsPage / settings-route gzip families. | #789 |
| Personal Performance page GETs | `hooks/usePersonalPerformanceQuery.ts` + `PersonalPerformancePage` | Imperative `fetchQuery` only (no live `useQuery` observer, no `useMutation`); query keys `['portfolio','performance','accounts']` and `['portfolio','performance','quality', accountId, 50]`; transport `portfolioApi.getAccounts(false)` then `getPaperDecisionQuality(id, {limit:50})`; `retry: false`; no poll; `refetchOnWindowFocus: false`; `networkMode: 'always'`; `staleTime: 0`; request-id owner loading/refreshing/error; React state for accounts/accountId/report (cache is transport-only); exact-key silent cancel (`silent: true, revert: false`) + `removeQueries`; mount accounts-then-quality; empty paper list skips quality; account change is quality-only; Refresh both; same-key refresh cancel+remove then `fetchQuery`; unmount exact-removes live keys; silent `CancelledError` does not `setError`; live-generation hard error clears `report` (unlike Token Usage last-good); newest generation wins | #789 |
| `BacktestPage` initial mount load | `hooks/useBacktestInitialLoadQuery.ts` / `BacktestPage.tsx` | One run per mount (`BACKTEST_INITIAL_LOAD_QUERY_KEY`); `retry: false`; no poll; `refetchOnWindowFocus: false`; `staleTime: 0`; `networkMode: 'always'`; page-owned `loadInitial` (performance then results) and page-owned generation-bump unmount cancel (`onCancelInFlight` + cleanup effect); existing page error surfaces. Verified on live main `b1feba2a` (#1615). | #789 (#1615) |
| `ChatPage` skills + context-compression mount loads | `hooks/useChatMountLoadsQuery.ts` / `ChatPage.tsx` | `useChatMountLoad` keys `['chat','skills']` and `['chat','context-compression']`; `retry: false`; no poll; `refetchOnWindowFocus: false`; `staleTime: 0`; `networkMode: 'always'`; page-owned loaders; abort-backed `isActive()` (`!signal.aborted`) state-write guard; skills catch logs then `setIsSkillsLoading(false)`; context-compression catch sets the page error surface. Verified on live main `afeb78b5` (#1616). Page imports the hook file directly (not the barrel). | #789 (#1616) |
| Research Analysis Workbench setup-status + analysis-skills | `hooks/useWorkbenchMountLoadsQuery.ts` / `ResearchAnalysisWorkbenchPage.tsx` | Independent `useQuery`s (`WORKBENCH_SETUP_STATUS_QUERY_KEY`, `WORKBENCH_ANALYSIS_SKILLS_QUERY_KEY`); `retry: false`; no poll; `refetchOnWindowFocus: false`; `staleTime: 0`; `networkMode: 'always'`; page-owned swallow-and-fallback (`setSetupComplete(null)`, `setAnalysisSkills([])`); abort-backed `isActive()` (`!signal.aborted`) state-write guard; setup `finally` still `setIsSetupStatusResolved(true)` when active | #789 |
| `StockScreeningPage` screen-task poll | `hooks/useScreenTaskPollQuery.ts` / `StockScreeningPage.tsx` | Settlement-anchored **2000ms** `refetchInterval` (`SCREEN_TASK_POLL_INTERVAL_MS` passed in; hook does not import `components/`); first poll immediate on enabled mount; in-flight polls do not overlap or cancel; `retry: false`; `refetchOnWindowFocus: false`; `refetchIntervalInBackground: true`; `networkMode: 'always'`; `staleTime: 0`; `restartKey: [language]` aborts the old generation and starts a new poll immediately; `enabled` drops when `finishTask()` nulls `activeTaskId`; poll body (status machine, unrecoverable vs recoverable, persist, `applyScreenResult`) stays page-owned. Page imports the hook file directly (not the barrel). This row does not close Issue #789. | #789 |
| Watchlist codes GET (`useWatchlist`) | `hooks/useWatchlist.ts` | Live `useQuery` around `systemConfigApi.getWatchlist()`; exact key `['watchlist','codes']`; `enabled` gate preserved (Decision Signals watchlist scope); `retry: false`; no poll; `refetchOnWindowFocus: false`; `networkMode: 'always'`; `staleTime: 0`; in-flight dedupe so concurrent Home / Workbench / Decision Signals observers share one GET; disable/re-enable uses exact-key settling cancellation (`silent: false, revert: true`) so `fetchStatus` returns from `fetching` and a stale completion cannot overwrite the successor; mutations stay hook-owned (`add`/`remove`/`toggle`/`isActioning`/`actionMessage`) and `setQueryData` the exact key after success (cancel in-flight GET first) so observers converge without a second GET scheduler; `refresh` is `fetchQuery` returning `Promise<boolean>`; queryFn treats abort/unmount/generation as Query-owned guards instead of throwing a silent `CancelledError`; `isLoading` is limited to an enabled initial GET with no settled data, so background refetch preserves usable codes and controls; last-observer unmount discards the exact row after a microtask (`cancelQueries` + `removeQueries`) so StrictMode remount keeps the GET while the next route owner starts fresh. Groups, scores, coverage, and Chat's raw `getWatchlist` stay out of this row. | #789 |
| `FinancialCalculatorsPage` three user-triggered POSTs | `hooks/useFinancialCalculatorsMutation.ts` / `FinancialCalculatorsPage.tsx` | `useMutation`; `retry: false`; no poll; no focus refetch; no query cache of POST bodies; abort + request generation so stale completions cannot write; `{ signal }` on `calculatorsApi.compoundGrowth` / `targetContribution` / `targetDuration`; page-owned form/validate/mode UI; page imports the hook file directly (not the barrel) | #789 |
| Report Version Compare list + compare GETs | `hooks/useReportVersionCompareQueries.ts` / `ReportVersionComparePage.tsx` | Imperative `fetchQuery` only (no live `useQuery` observer, no `useInfiniteQuery`, no `useMutation`); query keys `['report-version-compare','runs',stockCode,page,50]` and `['report-version-compare','compare',stockCode,baseRunId,targetRunId]`; transport `reportVersionCompareApi.listRuns` / `compare` with `{ signal }`; `retry: false`; no poll; `refetchOnWindowFocus: false` (overrides app-root default `true`); `networkMode: 'always'`; `staleTime: 0`; one request-id owner across both families; React state for runs/result (cache is transport-only); exact-key silent cancel (`silent: true, revert: false`) + `removeQueries`; same-key refresh cancel+remove then `fetchQuery`; unmount exact-removes live keys; validation-only empty/missing/same-id calls do not bump generation or cancel in-flight transport; a valid list start clears `comparing` and sets exactly one of `loadingRuns`/`loadingMore`; a valid compare start clears both list flags and sets `comparing`; silent `CancelledError` does not `setError`; newest generation wins. Page-owned draft stock, selected IDs, route-prefill parsing, one-shot auto-compare, empty-state UI, and document title. Page imports the hook file directly (not the barrel). | #789 |
| Home watchlist aggregate scores | `hooks/useWatchlistScores.ts` + `HomeWatchlistGroupsSection` | Imperative `fetchQuery` only (no live `useQuery` observer, no `useMutation`); query key `['watchlist','scores',codesKey,String(refreshKey)]` where `codesKey` is `JSON.stringify` of trimmed non-empty codes and `String(refreshKey)` is the single refresh identity for the request signature, Query key, and effect deps (number `1` and string `'1'` are one generation); transport `watchlistScoresApi.score({ stockCodes, sort: 'manual', signal })`; `retry: false`; no poll; `refetchOnWindowFocus: false` (overrides app-root default `true`); `networkMode: 'always'`; `staleTime: 0`; request-id owner; React state for `{status, itemsByCode}` (cache is transport-only); empty codes stay `idle` and skip fetch; `refreshKey` and reused signatures (`A→B→A`, `A→empty→A`) immediately expose `loading` plus an empty map and never resurrect a prior matching settled record; exact-key silent cancel (`silent: true, revert: false`) + `removeQueries`; same-key refresh cancel+remove then `fetchQuery`; unmount/key change exact-removes live scores keys and must not prefix-cancel `['watchlist']` or the live `['watchlist','codes']` GET; silent `CancelledError` / superseded abort does not surface `error`; live-generation hard error is `error` plus empty data with no stale fallback; response items are filtered to requested codes; `createUnanalyzedWatchlistScore` stays exported. Home imports the hook file directly (not the barrel). Session / groups / coverage stay out of this row. | #789 |
| Home watchlist groups GET (`useWatchlistGroups`) | `hooks/useWatchlistGroups.ts` | Live `useQuery` around `watchlistGroupsApi.list()`; exact key `['watchlist','groups']`; `enabled` gate; `retry: false`; no poll; `refetchOnWindowFocus: false`; `networkMode: 'always'`; `staleTime: 0`; in-flight dedupe so concurrent observers share one GET; disable/re-enable uses exact-key settling cancellation (`silent: false, revert: true`) so `fetchStatus` returns from `fetching` and a stale axios completion cannot overwrite the successor; `refresh` is `fetchQuery` returning `Promise<boolean>` and keeps the previous first-load / explicit-refresh loading surface; queryFn treats abort/unmount/generation as Query-owned guards instead of throwing a silent `CancelledError`; mutations stay hook-owned (synchronous `actionLeaseRef`, revision-CAS, `isActioning`, `errorMessage`) and must not move to `useMutation`; mutation success exact-cancels the competing groups GET then `setQueryData` the exact key so observers converge without a second GET; mutation failure keeps the original action error (`watchlist.groupsActionFailed` or the parsed mutation message) and schedules one recovery `list()` through the same Query — recovery success may restore groups/revision but must not clear or replace the action error, and recovery failure must not replace it either; last-observer unmount discards the exact row after a microtask (`cancelQueries` + `removeQueries`) and must not prefix-cancel `['watchlist']`, `['watchlist','codes']`, or `['watchlist','scores',…]`. Coverage and Settings section-local loaders stay out of this row. | #789 |
| Research Analysis Workbench watchlist coverage fallback (`useWatchlistAnalysisCoverage`) | `hooks/useWatchlistAnalysisCoverage.ts` / `ResearchAnalysisWorkbenchPage.tsx` | Imperative `fetchQuery` only (no live `useQuery` observer, no `useInfiniteQuery`, no `useMutation`); query key `['watchlist','coverage','history', signature]` where `signature` is the missing-from-stock-bar normalized code keys joined by `'\n'`; transport `historyApi.getList({ stockCode, limit: 1 }, { signal })`; 4-worker pool stays **inside** `queryFn` (`WATCHLIST_HISTORY_LOOKUP_CONCURRENCY`); `retry: false`; no poll; `refetchOnWindowFocus: false` (overrides app-root default `true`); `networkMode: 'always'`; `staleTime: 0`; request-id owner; React state for `{rows, analyzedTodayCount, pendingCodes, isTodayStatusBlocked}` (cache is transport-only); skip transport while stock-bar is loading or the initial stock-bar load is unsettled; lookup only normalized watchlist codes missing from the stock bar (`MARKET` excluded); signature change, stock-bar reload, or unmount aborts the live generation; exact-key silent cancel (`silent: true, revert: false`) + `removeQueries`; same-key refresh cancel+remove then `fetchQuery`; unmount/signature change exact-removes live coverage keys and must not prefix-cancel `['watchlist']`, `['watchlist','codes']`, `['watchlist','scores',…]`, or `['watchlist','groups']`; silent `CancelledError` / `AbortError` does not mark unknown/failed or write a superseded map; reused missing-code signatures (`empty → bar → empty`) immediately block until the new lookup settles and never resurrect a prior matching settled record; stale success must not clear a newer `stockBarRefreshFailed` unknown; Workbench pending-only (`pendingBlocked` / `pendingCodes`) stays hook-owned. Page imports the hook file directly (not the barrel). Groups / scores / Settings section-local loaders stay out of this row. | #789 |
| Settings Signal Scorecard public preview GET (`useSignalScorecardQuery`) | `hooks/useSignalScorecardQuery.ts` / `SignalScorecardPanel.tsx` | Imperative `fetchQuery` only (no live `useQuery` observer, no `useInfiniteQuery`, no `useMutation`); exact key `['scorecard','public']`; transport `scorecardApi.getPublic()` with no AbortSignal; `retry: false`; no poll; `refetchOnWindowFocus: false` (overrides app-root default `true`); `networkMode: 'always'`; `staleTime: 0`; request-id owner; React state for `{data, isLoading, isRefreshing, loadError}` (cache is transport-only); skip `getPublic` while `publicEnabled` is false and exact-cancel+remove plus clear display state; the panel `disabled` prop still only disables refresh; 404/`not_found` stays disabled-empty via the panel helper; other errors fail closed (`data = null`, parsed `ApiErrorAlert`); exact-key silent cancel (`silent: true, revert: false`) + `removeQueries`; same-key refresh cancel+remove then `fetchQuery`; unmount/disable exact-removes the public key and must not prefix-cancel `['scorecard']`; silent `CancelledError` does not `setLoadError` and is not classified as disabled-empty; newest generation wins across disable, unmount, refresh, and stale 200 versus newer 404. Panel imports the hook file directly (not the barrel). Remaining Settings section-local loaders stay out of this row. This row does not close Issue #789. | #789 |
| Settings data-provider runtime status GET (`useDataProviderRuntimeStatusQuery`) | `hooks/useDataProviderRuntimeStatusQuery.ts` / `DataProviderRuntimeStatusPanel.tsx` | Imperative `fetchQuery` only (no live `useQuery` observer, no `useInfiniteQuery`, no `useMutation`); exact key `['data-providers','runtime-status']`; transport `systemConfigApi.getDataProviderRuntimeStatus()` with no AbortSignal; `retry: false`; no poll; `refetchOnWindowFocus: false` (overrides app-root default `true`); `networkMode: 'always'`; `staleTime: 0`; request-id owner; React state for `{status, isLoading, error}` (cache is transport-only); `isLoading` only (no `isRefreshing`); mount and refresh both set `isLoading`; `disabled` does not skip fetch and only disables the refresh button; 200-partial (`partial === true` / `sourceState !== 'ok'`) is success and keeps the existing partial Surface; transport/HTTP errors fail closed (`status = null`, parsed `ApiErrorAlert`); exact-key silent cancel (`silent: true, revert: false`) + `removeQueries`; same-key refresh cancel+remove then `fetchQuery`; unmount exact-removes the runtime key and must not prefix-cancel `['data-providers']` or `['settings']`; silent `CancelledError` does not `setError` and is not classified as empty (`data-runtime-empty` is `!isLoading && !error && !status` only); newest generation wins across unmount, refresh, stale 200 versus newer 500, and stale 500 versus newer partial. Panel imports the hook file directly (not the barrel). Remaining Settings section-local loaders stay out of this row. The tracker remains open. | #789 |
| Settings Kronos status GET (`useKronosStatusQuery`) | `hooks/useKronosStatusQuery.ts` / `KronosStatusPanel.tsx` | Imperative `fetchQuery` only (no live `useQuery` observer, no `useInfiniteQuery`, no `useMutation`); exact key `['kronos','status']`; transport `systemConfigApi.getKronosStatus()` with no AbortSignal; `retry: false`; no poll; `refetchOnWindowFocus: false` (overrides app-root default `true`); `networkMode: 'always'`; `staleTime: 0`; request-id owner; React state for `{status, isLoading, error}` (cache is transport-only); `isLoading` only (no `isRefreshing`); mount and refresh both set `isLoading`; `disabled` does not skip fetch and only disables the refresh button; 200 needs-action (`ready === false` / `enabled === false` / missing deps / `packagedDesktop` / `!installSupported`) is success and keeps the existing badges + `SettingsAlert`; transport/HTTP errors fail closed (`status = null`, parsed `ApiErrorAlert`); exact-key silent cancel (`silent: true, revert: false`) + `removeQueries`; same-key refresh cancel+remove then `fetchQuery`; unmount exact-removes the status key and must not prefix-cancel `['kronos']`, `['settings']`, `['data-providers']`, or `['scorecard']`; silent `CancelledError` does not `setError` and is not classified as empty (`!isLoading && !error && !status` only); newest generation wins across unmount, refresh, stale 200 versus newer 500, and stale 500 versus newer needs-action. Panel imports the hook file directly (not the barrel). Remaining Settings section-local loaders stay out of this row. The tracker remains open. | #789 |
| Settings outbound-activity GET (`useOutboundActivityQuery`) | `hooks/useOutboundActivityQuery.ts` / `OutboundActivityPanel.tsx` | Imperative `fetchQuery` only (no live `useQuery` observer, no `useInfiniteQuery`, no `useMutation`); exact key `['outbound-activity','load']`; transport one atomic `Promise.all` of `getLocalOnlyStatus()` and `listActivity({limit:50})` with no AbortSignal; `retry: false`; no poll; `refetchOnWindowFocus: false` (overrides app-root default `true`); `networkMode: 'always'`; `staleTime: 0`; request-id owner; React state for `{status, items, isLoading, isRefreshing, loadError}` (cache is transport-only); preserve initial vs refresh UI and prior rows during refresh; `disabled` does not skip fetch and only disables the refresh button; empty 200 (`items: []`) is success and keeps EmptyState; other errors atomically clear both status and items and parse `ApiErrorAlert`; exact-key silent cancel (`silent: true, revert: false`) + `removeQueries`; same-key refresh cancel+remove then `fetchQuery`; unmount exact-removes the load key and must not prefix-cancel `['outbound-activity']` or `['settings']`; silent `CancelledError` does not `setLoadError` and does not clear a newer generation; newest generation wins. Panel imports the hook file directly (not the barrel). Remaining Settings section-local loaders stay out of this row. The tracker remains open. | #789 |
| Settings security-audit list GET (`useSecurityAuditQuery`) | `hooks/useSecurityAuditQuery.ts` / `SecurityAuditPanel.tsx` | Imperative `fetchQuery` only (no live `useQuery` observer, no `useInfiniteQuery`, no `useMutation`); exact key `['security-audit','events', page, pageSize, eventTypeTrimmed \|\| 'all', outcome \|\| 'all', correlationTrimmed \|\| 'none']`; transport `securityAuditApi.list(buildQuery(…))` with no AbortSignal and no `occurredFrom`/`occurredTo`; `retry: false`; no poll; `refetchOnWindowFocus: false` (overrides app-root default `true`); `networkMode: 'always'`; `staleTime: 0`; request-id owner; React state for `{items, page, pageSize, total, isLoading, isRefreshing, loadError}` (cache is transport-only); draft filters stay panel-owned; language is parse-only and not a key element; mount always fetches page 1 / pageSize 50 / empty filters including when `disabled === true`; preserve initial vs refresh UI and prior rows during refresh; empty 200 (`items: []` / `total: 0`) is success EmptyState; HTTP errors fail closed (`items = []`, `total = 0`, parsed `loadError`); `security_audit_auth_required` stays a parsed error and the panel maps only that code to blocked StatePanel (generic 403 stays `ApiErrorAlert`); exact-key silent cancel (`silent: true, revert: false`) + `removeQueries`; same-key refresh cancel+remove then `fetchQuery`; page/filter change cancel+remove the previous exact key then `fetchQuery` the new key; unmount exact-removes the live key and must not prefix-cancel `['security-audit']`, `['settings']`, `['outbound-activity']`, `['kronos']`, `['data-providers']`, `['scorecard']`, or `['notifications']`; silent `CancelledError` does not `setLoadError` and does not clear a newer generation; newest generation wins. Panel imports the hook file directly (not the barrel). Remaining Settings section-local loaders stay out of this row. The tracker remains open. | #789 |
| Settings runtime-capabilities inventory GET + Agent deployment-models GET (`useRuntimeCapabilitiesQuery`) | `hooks/useRuntimeCapabilitiesQuery.ts` / `RuntimeCapabilitiesPanel.tsx` | Imperative `fetchQuery` only (no live `useQuery` observer, no `useInfiniteQuery`, no `useMutation`); exact keys `['capabilities','runtime']` and `['agent-models','deployments']`; transport `capabilitiesApi.list()` and `agentApi.getModels()` with no AbortSignal; `retry: false`; no poll; `refetchOnWindowFocus: false` (overrides app-root default `true`); `networkMode: 'always'`; `staleTime: 0`; independent request-id owners per key; React state for `{capabilities, capabilitiesLoading, capabilitiesError, models, modelsLoading, modelsError}` (cache is transport-only); `*Loading` only (no `isRefreshing`); mount always fetches both GETs; independent reload (reload A must not cancel/remove key B); language is parse-only and not a key element; partial 200 (`partial === true` / source `state !== 'ok'`) is success and keeps SummaryStrip + source badges; empty 200 (`items: []` / `total: 0` and `models: []`) is success emptyState; initial HTTP errors fail closed (`capabilities === null` / `models === null`, DataTable error); refresh HTTP errors keep the last-good snapshot and parse `SettingsAlert`; exact-key silent cancel (`silent: true, revert: false`) + `removeQueries`; same-key refresh cancel+remove then `fetchQuery`; unmount exact-removes both live keys and must not prefix-cancel `['capabilities']`, `['agent-models']`, `['settings']`, `['security-audit']`, `['outbound-activity']`, `['kronos']`, `['data-providers']`, `['scorecard']`, `['notifications']`, or `['agent-run-feedback']`; silent `CancelledError` does not `set*Error` and does not clear last-good; newest generation wins per key. Panel imports the hook file directly (not the barrel). Remaining Settings section-local loaders stay out of this row. The tracker remains open. | #789 |
| Settings scheduler status GET (`useSchedulerStatusQuery`) | `hooks/useSchedulerStatusQuery.ts` / `SchedulerSettingsCard.tsx` | Imperative `fetchQuery` only (no live `useQuery` observer, no `useInfiniteQuery`, no `useMutation`); exact key `['scheduler','status']`; transport `systemConfigApi.getSchedulerStatus()` with no AbortSignal; `retry: false`; no Query `refetchInterval`; `refetchOnWindowFocus: false` (overrides app-root default `true`); `networkMode: 'always'`; `staleTime: 0`; request-id owner; React state for `{status, isRefreshingStatus, statusError}` (cache is transport-only); `isRefreshingStatus` only (no `isLoading`); mount, parent `statusRefreshToken` change, and the refresh button share one `refresh()` path; skip GET while `hasSchedulerSettings` is false and exact-cancel+remove; the card `disabled` prop does not skip fetch and only disables refresh/run-now; HTTP errors keep last-good status and parse `ApiErrorAlert`; exact-key silent cancel (`silent: true, revert: false`) + `removeQueries`; same-key refresh cancel+remove then `fetchQuery`; unmount/disable exact-removes the status key and must not prefix-cancel `['scheduler']` (versioned scheduled-tasks list GET uses disjoint `['scheduled-tasks','list']`); silent `CancelledError` does not `setStatusError` and does not overwrite a newer generation; newest generation wins across unmount, token refresh, stale 200 versus newer 500, and stale 500 versus newer 200. The 3s in-run poll (`RUNNING_STATUS_POLL_MS = 3000`) stays card-owned and calls `refresh()` only while `status.running || trackedRun.state === 'running'`. Tracked-run derivation and `runSchedulerNow()` stay card-owned. The overlap probe is scheduled by `useScheduledTasksOverlapQuery` on a disjoint two-element `['scheduled-tasks','overlap']` key. Card imports the hook file directly (not the barrel). Remaining Settings section-local loaders stay out of this row. The tracker remains open. | #789 |
| Settings loaded-extensions list GET (`useLoadedExtensionsQuery`) | `hooks/useLoadedExtensionsQuery.ts` / `LoadedExtensionsPanel.tsx` | Imperative `fetchQuery` only (no live `useQuery` observer, no `useInfiniteQuery`, no `useMutation`); exact key `['plugins','list']`; transport `pluginsApi.list()` with no AbortSignal; `retry: false`; no Query `refetchInterval`; `refetchOnWindowFocus: false` (overrides app-root default `true`); `networkMode: 'always'`; `staleTime: 0`; request-id owner; React state for `{items, total, isLoading, isRefreshing, loadError}` (cache is transport-only); language is parse-only and not a key element; mount always fetches including when `disabled === true`; the panel `disabled` prop does not skip GET and only disables lifecycle/settings/refresh controls; preserve initial vs refresh UI and prior rows during refresh; empty 200 (`items: []` / `total: 0`) is success EmptyState; initial HTTP errors fail closed (`items = []`, `total = 0`, parsed `loadError`); refresh HTTP errors keep the last-good roster and parse `ApiErrorAlert`; exact-key silent cancel (`silent: true, revert: false`) + `removeQueries`; same-key refresh cancel+remove then `fetchQuery`; unmount exact-removes the list key and must not prefix-cancel `['plugins']`; silent `CancelledError` does not `setLoadError` and does not overwrite a newer generation; newest generation wins across unmount, refresh, stale 200 versus newer 500, and stale 500 versus newer 200. Search-param plugin row scroll, `updateLifecycle` enable/disable (including the optimistic `setItems` patch before the follow-up GET), `getSettings`/`updateSettings`/modal, notification deep links, trust copy, empty-state/i18n/DataTable columns stay panel-owned. Panel stays lazy in `SystemSecuritySection` and imports the hook file directly (not the barrel). Remaining Settings section-local loaders stay out of this row. The tracker remains open. | #789 |
| Settings scheduled-tasks list GET (`useScheduledTasksListQuery`) | `hooks/useScheduledTasksListQuery.ts` / `ScheduledTasksPanel.tsx` | Imperative `fetchQuery` only (no live `useQuery` observer, no `useInfiniteQuery`, no `useMutation`); exact key `['scheduled-tasks','list']`; transport `scheduledTasksApi.list({ limit: 200 })` with no AbortSignal; `retry: false`; no Query `refetchInterval`; `refetchOnWindowFocus: false` (overrides app-root default `true`); `networkMode: 'always'`; `staleTime: 0`; request-id owner; React state for `{items, isLoading, isRefreshing, loadError}` (cache is transport-only; no `total`); language is parse-only and not a key element; mount always fetches including when `disabled === true`; the panel `disabled` prop does not skip GET and only disables create/refresh/toggle/history controls; preserve initial vs refresh UI and prior rows during refresh; empty 200 (`items: []`) is success EmptyState; initial HTTP errors fail closed (`items = []`, parsed `loadError`); refresh HTTP errors keep the last-good roster and parse `ApiErrorAlert`; exact-key silent cancel (`silent: true, revert: false`) + `removeQueries`; same-key refresh cancel+remove then `fetchQuery`; unmount exact-removes the list key and must not prefix-cancel `['scheduled-tasks']`, `['scheduler']`, `['plugins']`, `['settings']`, `['watchlist']`, or Home keys; silent `CancelledError` does not `setLoadError` and does not overwrite a newer generation; newest generation wins across unmount, refresh, stale 200 versus newer 500, and stale 500 versus newer 200. `load()` returns the successful items array (including empty) and `undefined` on cancel/error so the panel can call `loadLatestRuns(next)` only after a successful list load; `setItems` stays exposed for panel-owned enable/disable patches. The per-row getStatus fan-out is scheduled by `useScheduledTaskLatestRunsQuery` on a disjoint two-element `['scheduled-tasks','latest-runs']` key. Mutations, create modal, i18n, table, empty/error surfaces stay panel-owned. History/`listRuns` is scheduled by `useScheduledTaskRunHistoryQuery` on a disjoint four-element `runs` key. The sibling overlap probe is scheduled by `useScheduledTasksOverlapQuery` on a disjoint two-element `['scheduled-tasks','overlap']` key and must not share this key. Panel stays lazy in `SystemSecuritySection` and imports the hook file directly (not the barrel). Remaining Settings section-local loaders stay out of this row. The tracker remains open. | #789 |
| Settings config-presets list GET (`useConfigPresetsListQuery`) | `hooks/useConfigPresetsListQuery.ts` / `ConfigPresetsPanel.tsx` | Imperative `fetchQuery` only (no live `useQuery` observer, no `useInfiniteQuery`, no `useMutation`); exact key `['config-presets','list']`; transport `configProfilesApi.listPresets()` with no params or AbortSignal; `retry: false`; no Query `refetchInterval`; `refetchOnWindowFocus: false` (overrides app-root default `true`); `networkMode: 'always'`; `staleTime: 0`; request-id owner; React state for `{presets, recommendedId, isLoading, loadError}` (cache is transport-only; no `isRefreshing`, `total`, or `detection` display); language is parse-only and not a key element; `configVersion` is used by preview/apply/import only and is absent from the list key; mount always fetches including when `disabled === true`; the panel `disabled` prop does not skip GET and only disables apply/refresh/export/import controls; empty 200 (`presets: []`) is success EmptyState; initial and refresh HTTP errors both fail closed (`presets = []`, `recommendedId = null`, parsed `loadError`); exact-key silent cancel (`silent: true, revert: false`) + `removeQueries`; same-key refresh cancel+remove then `fetchQuery`; unmount exact-removes the list key and must not prefix-cancel `['config-presets']`, `['settings']`, `['plugins']`, `['scheduler']`, `['scheduled-tasks']`, `['watchlist']`, or Home keys; silent `CancelledError` does not `setLoadError` and does not overwrite a newer generation; newest generation wins across unmount, refresh, stale 200 versus newer 500, and stale 500 versus newer 200. `load()` returns successful `{ presets, recommendedPresetId }` (including empty presets) and `undefined` on cancel/error. No optimistic setter. Preview/apply/export/import/dialog flows stay panel-owned; apply/import still call `onApplied(updatedKeys)` then reload. Panel stays lazy in `SettingsPage` with `Suspense fallback={<SettingsLoading />}` around the existing panel JSX only (ConfigBackupCard stays a static sibling) and imports the hook file directly (not the barrel). Remaining Settings section-local loaders stay out of this row. The tracker remains open. | #789 |
| Settings investment-framework current GET (`useInvestmentFrameworkQuery`) | `hooks/useInvestmentFrameworkQuery.ts` / `InvestmentFrameworkSettingsCard.tsx` | Imperative `fetchQuery` only (no live `useQuery` observer, no `useInfiniteQuery`, no `useMutation`); exact key `['investment-framework','current']`; transport `investmentFrameworkApi.get()` with no params or AbortSignal; `retry: false`; no Query `refetchInterval`; `refetchOnWindowFocus: false` (overrides app-root default `true`); `networkMode: 'always'`; `staleTime: 0`; request-id owner; React state for `{framework, exists, isLoading, loadError}` (cache is transport-only; no `isRefreshing`); language is prompt-preview-only and is not a key element; there is no disabled gate; mount always fetches once; 200 sets `framework` and `exists=true`; 404 or `investment_framework_not_found` is a successful missing sentinel (`framework=null`, `exists=false`, no load error); other HTTP failures set parsed `loadError`, do not mark missing, and do not clear the last successful `framework`/`exists` state; exact-key silent cancel (`silent: true, revert: false`) + `removeQueries`; same-key refresh cancel+remove then `fetchQuery`; unmount exact-removes the current key and must not prefix-cancel `['investment-framework']`, `['settings']`, `['config-presets']`, `['scheduled-tasks']`, `['scheduler']`, `['plugins']`, `['watchlist']`, or Home keys; silent `CancelledError` / stale generations never set `loadError` and never overwrite a newer generation; newest generation wins across unmount, refresh, stale 200 versus newer 500, stale 500 versus newer 200, and stale 200 versus newer missing. `load()` returns successful `{ framework, exists }` (including missing) and `undefined` on cancel/error. `setFramework` / `setExists` stay exposed so create/update/deactivate/remove patch display state from mutation responses without a re-GET. Draft replace, conflict reload (`load(true)`), history GET, editor models, and prompt preview stay card-owned; history remains lazy-open and panel-owned. Card stays lazy in `SettingsPage` with `Suspense fallback={<SettingsLoading />}` around the existing card JSX only and imports the hook file directly (not the barrel). Remaining Settings section-local loaders stay out of this row. The tracker remains open. | #789 |
| Settings Local Models catalog GET (`useLocalModelsCatalogQuery`) | `hooks/useLocalModelsCatalogQuery.ts` / `LocalModelsPanel.tsx` | Imperative `fetchQuery` only (no live `useQuery` observer, no `useInfiniteQuery`, no `useMutation`); exact key `['local-models','catalog']`; transport `localModelsApi.getCatalog()` with no params or AbortSignal; `retry: false`; no Query `refetchInterval`; `refetchOnWindowFocus: false` (overrides app-root default `true`); `networkMode: 'always'`; `staleTime: 0`; request-id owner; Query cache is transport-only. Combined catalog+runtime initialization stays panel-owned (`Promise.all` of `loadCatalog()` and `loadRuntime()`). Runtime GET is scheduled by `useLocalModelsRuntimeQuery` on a disjoint two-element `['local-models','runtime']` key. React state for `{models, runtime, isLoading, catalogFailed}` stays in the panel. There is no enabled gate and language is display-only, not a key element. Mount always fetches catalog and runtime together; empty 200 (`models: []`) is success section EmptyState; catalog or runtime failure is `catalogFailed` (not a successful empty catalog) and does not clear last-good `models`/`runtime`. Catalog retry is the failed EmptyState refresh, which reloads both catalog and runtime; the header refresh icon stays runtime-only. Exact-key silent cancel (`silent: true, revert: false`) + `removeQueries`; same-key refresh cancel+remove then `fetchQuery` only while this hook is the sole living owner; a second living owner joins the in-flight exact-key `fetchQuery` and must not cancel or remove it; unmount exact-removes the catalog key only when it is the last living owner and must not prefix-cancel `['local-models']`, `['settings']`, `['config-presets']`, `['investment-framework']`, `['scheduled-tasks']`, `['scheduler']`, `['plugins']`, `['watchlist']`, `['kronos']`, or Home keys; silent `CancelledError` / stale generations never set `catalogFailed` and never overwrite a newer generation. Runtime detection/subscription/polling, pull/import/delete/assignment/configuration mutations, Kronos, desktop/browser `LocalModelTransport`, and Local Only stay panel/transport-owned. Panel imports the hook file directly (not the barrel). `LocalModelsWithKronos` and `FirstRunWizard` lazy-load the panel around existing conditional JSX with `Suspense` so the hook is not billed to frozen Settings gzip families. Remaining Settings section-local loaders stay out of this row. The tracker remains open. | #789 |
| Settings Local Models runtime GET (`useLocalModelsRuntimeQuery`) | `hooks/useLocalModelsRuntimeQuery.ts` / `LocalModelsPanel.tsx` | Imperative `fetchQuery` only (no live `useQuery` observer, no `useInfiniteQuery`, no `useMutation`); exact two-element key `['local-models','runtime']`; inject panel transport `getRuntime: () => Promise<LocalModelRuntimeState>` (do not import `localModelTransport` into `hooks/`; do not call `localModelsApi.getRuntime()` from the hook, which would skip desktop `bridge.detect`); Query `signal` is used only for `throwIfCancelled` after `getRuntime()` (transport has no AbortSignal); `retry: false`; no Query `refetchInterval`; `refetchOnWindowFocus: false` (overrides app-root default `true`); `networkMode: 'always'`; `staleTime: 0`; cloned catalog dual-owner counting (`WeakMap<QueryClient, { ownerCount, fetchGeneration }>`); Query cache is transport-only. Combined catalog+runtime initialization stays panel-owned (`Promise.all` of `loadCatalog()` and `loadRuntime()`); React state for `{models, runtime, isLoading, catalogFailed, actionError}` stays in the panel. There is no enabled gate, no mount auto-GET inside the hook, and language is display-only, not a key element. Mount always fetches catalog and runtime together; empty catalog 200 (`models: []`) is success section EmptyState; catalog or runtime failure is `catalogFailed` (not a successful empty catalog) and does not clear last-good `models`/`runtime`. Header / mutation follow-up `refreshRuntime()` calls `loadRuntime()` only; failure sets `actionError` and keeps last-good `models` / `runtime` without setting `isLoading` / `catalogFailed`. Exact-key silent cancel (`silent: true, revert: false`) + `removeQueries`; same-key refresh cancel+remove then `fetchQuery` only while this hook is the sole living owner; a second living owner joins the in-flight exact-key `fetchQuery` and must not cancel or remove it; unmount exact-removes the runtime key only when it is the last living owner and must not prefix-cancel `['local-models']` or exact-cancel `['local-models','catalog']`, `['settings']`, `['generation-backend']`, `['intelligence']`, `['config-presets']`, `['investment-framework']`, `['scheduled-tasks']`, `['scheduler']`, `['plugins']`, `['watchlist']`, `['scorecard']`, `['kronos']`, `['data-providers']`, `['outbound-activity']`, `['security-audit']`, `['capabilities']`, `['agent-models']`, `['notifications']`, or Home keys; silent `CancelledError` / stale generations never set `catalogFailed` or `actionError` and never overwrite a newer generation. Focus/visibility detect-after-install, `transport.subscribe`, pull/import/delete/assignment/configuration mutations, Kronos, desktop/browser `LocalModelTransport`, and Local Only stay panel/transport-owned. `LLMChannelEditor` raw `getRuntime()` and raw `getGenerationBackendStatus()` stay editor-owned and must not share this key. Panel imports the hook file directly (not the barrel). `LocalModelsWithKronos` and `FirstRunWizard` lazy-load the panel around existing conditional JSX with `Suspense` so the hook bills to `LocalModelsPanel-*` under the default gzip cap rather than frozen SettingsPage / settings-route families. Remaining Settings section-local loaders stay out of this row. Issue #789 stays open. | #789 |
| Settings generation-backend saved GET + draft preview POST (`useGenerationBackendStatusQuery`) | `hooks/useGenerationBackendStatusQuery.ts` / `GenerationBackendStatusPanel.tsx` | Imperative `fetchQuery` only (no live `useQuery` observer, no `useInfiniteQuery`, no `useMutation`); exact saved key `['generation-backend','status','saved']` (GET `systemConfigApi.getGenerationBackendStatus()`, no `maskToken`); exact preview key `['generation-backend','status','preview', fingerprint, maskToken]` where `fingerprint` is `JSON.stringify({key,value}[])` (POST `previewGenerationBackendStatus({ items, maskToken })`); empty `items` is saved GET; non-empty `items` is preview POST; `retry: false`; no Query `refetchInterval`; `refetchOnWindowFocus: false` (overrides app-root default `true`); `networkMode: 'always'`; `staleTime: 0`; request-id owner; React state for `{status, isLoading, error}` (cache is transport-only; no `isRefreshing`); `isLoading` on both mount and refresh; `disabled` does not skip fetch and only disables controls; language is display-only and is not a key element. First mount loads immediately (including when a draft already exists); subsequent identity changes (`items` / `maskToken`) wait 500 ms (`GENERATION_BACKEND_STATUS_PREVIEW_DEBOUNCE_MS`) then `load()`; effect cleanup only `clearTimeout` and does not bump generation or cancel in-flight HTTP, so an in-flight saved/preview completion may still apply during the debounce window; the refresh button calls `load()` immediately. Transport/HTTP errors fail closed (`status = null`, parsed `ApiErrorAlert`) and must not keep last-good rows; silent `CancelledError` does not `setError` and does not clear a newer generation; newest generation wins. Exact-key silent cancel (`silent: true, revert: false`) + `removeQueries`; same-key refresh cancel+remove then `fetchQuery`; key change at `load()` exact-removes the previous live key and the successor key; unmount exact-removes the live key and must not prefix-cancel `['generation-backend']`, `['local-models']`, `['settings']`, `['config-presets']`, `['investment-framework']`, `['scheduled-tasks']`, `['scheduler']`, `['plugins']`, `['watchlist']`, `['scorecard']`, `['kronos']`, `['data-providers']`, `['outbound-activity']`, `['security-audit']`, `['capabilities']`, `['agent-models']`, or Home keys. JSON smoke POST (`testGenerationBackend`) stays panel-owned and is excluded from the hook/`queryFn`; smoke start bumps generation so an in-flight GET/preview cannot overwrite smoke-applied rows; `setStatus` patches display without a Query write. Raw CLI probe in `LLMChannelEditor` keeps calling `getGenerationBackendStatus()` with a local request id and must not share the saved Query key. Draft merge stays on `mergeGenerationBackendDraftItems`. Panel stays lazy in `SettingsPage` with `Suspense fallback={<SettingsLoading />}` around the existing panel JSX only (`LLMConfigModeBanner` stays a static sibling) so the hook is billed to a `GenerationBackendStatusPanel-*` chunk under the default gzip cap rather than frozen `SettingsPage` / `settings-route` families. Panel imports the hook file directly (not the barrel). Remaining Settings section-local loaders stay out of this row. Issue #789 stays open. | #789 |
| Settings intelligence sources list GET + templates list GET (`useIntelligenceSourcesQuery`) | `hooks/useIntelligenceSourcesQuery.ts` / `IntelligenceSourcesPanel.tsx` | Imperative `fetchQuery` only (no live `useQuery` observer, no `useInfiniteQuery`, no `useMutation`); exact keys `['intelligence','sources','list']` and `['intelligence','templates','list']`; transport `intelligenceApi.listSources({ pageSize: 100 })` and `intelligenceApi.listTemplates()` with no AbortSignal; `pageSize: 100` is a caller constant baked into `queryFn`, not a key element; `retry: false`; no Query `refetchInterval`; `refetchOnWindowFocus: false` (overrides app-root default `true`); `networkMode: 'always'`; `staleTime: 0`; independent request-id owners per key; Query cache is transport-only. Atomic mount `Promise.all` of `loadSources()` and `loadTemplates()` plus React state for `{phase, sources, templates, loadError}` stay panel-owned; a templates 500 after sources succeed must not paint the sources list. Mutation reload (`create` / defaults / template add / live fetch / fetch-all) calls `loadSources()` only and does not set `phase='loading'`; sources GET failure still `setPhase('error')`. Language is parse-only for `getParsedApiError` / display copy and is not a Query key element; the panel `load` callback still depends on `language` so a language switch retriggers both GETs. There is no enabled gate, poll, or `disabled` prop. Empty 200 (`items: []`) is success EmptyState; empty templates omit the templates section. Exact-key silent cancel (`silent: true, revert: false`) + `removeQueries`; same-key refresh cancel+remove then `fetchQuery`; independent reload (reload sources must not cancel/remove the templates key and vice versa); unmount exact-removes both live keys and must not prefix-cancel `['intelligence']`, `['generation-backend']`, `['local-models']`, `['settings']`, `['plugins']`, `['config-presets']`, `['investment-framework']`, `['scheduled-tasks']`, `['scheduler']`, `['watchlist']`, `['scorecard']`, `['kronos']`, `['data-providers']`, `['outbound-activity']`, `['security-audit']`, `['capabilities']`, `['agent-models']`, or Home keys; silent `CancelledError` / stale generations never `setPhase('error')` and never overwrite a newer generation. `createSource`, `testSource`, `createDefaultSources`, `createSourceFromTemplate`, `fetchSource`, and `fetchEnabledSources` stay panel-owned. The items list GET is scheduled by `useIntelligenceItemsQuery` on a disjoint three-element `['intelligence','items','list']` key. Panel stays lazy in `SettingsPage` with `Suspense fallback={<SettingsLoading />}` around the existing panel JSX only so the hook is billed to an `IntelligenceSourcesPanel-*` chunk under the default gzip cap rather than frozen `SettingsPage` / `settings-route` families. Panel imports the hook file directly (not the barrel). Remaining Settings section-local loaders stay out of this row. Issue #789 stays open. | #789 |
| Settings notification-channel plugin roster GET (`useNotificationChannelPluginsQuery`) | `hooks/useNotificationChannelPluginsQuery.ts` / `NotificationChannelsPanel.tsx` | Imperative `fetchQuery` only (no live `useQuery` observer, no `useInfiniteQuery`, no `useMutation`); exact key `['plugins','notification-channels']`; transport `pluginsApi.list()` with no arguments and no AbortSignal; `retry: false`; no Query `refetchInterval`; `refetchOnWindowFocus: false` (overrides app-root default `true`); `networkMode: 'always'`; `staleTime: 0`; request-id owner; React state for `{items, isLoading, loadFailed}` (cache is transport-only; no `load()`, `isRefreshing`, `total`, `setItems`, or parsed `loadError`). Language is display-only and is not a Query key element; a language switch does not refetch. There is no enabled gate, poll, focus refetch, or refresh path. Mount always fetches including when the panel `disabled` prop is true; `disabled` only disables field edits, send-test, and bind controls. Empty 200 (`items: []`) is success EmptyState; HTTP errors fail closed (`items = []`, boolean `loadFailed` warning `InlineAlert`, never invent plugin cards, not `ApiErrorAlert`). Exact-key silent cancel (`silent: true, revert: false`) + `removeQueries`; same-key successor cancel+remove then `fetchQuery`; unmount exact-removes this key only and must not prefix-cancel `['plugins']` or exact-cancel loaded-extensions `['plugins','list']`, `['intelligence']`, `['generation-backend']`, `['local-models']`, `['settings']`, `['config-presets']`, `['investment-framework']`, `['scheduled-tasks']`, `['scheduler']`, `['watchlist']`, `['scorecard']`, `['kronos']`, `['data-providers']`, `['outbound-activity']`, `['security-audit']`, `['capabilities']`, `['agent-models']`, `['notifications']`, or Home keys; silent `CancelledError` / stale generations never set `loadFailed` and never overwrite a newer generation; newest generation wins across unmount, StrictMode remount, stale 200 versus newer 500, and stale 500 versus newer empty list. Do not share the loaded-extensions key or import that hook. Channel test (`systemConfigApi.testNotificationChannel`), bind-events, DingTalk grouping, 30s `statusNow` interval, focused-channel scroll, builtin modal, and `collectPluginNotificationChannelLinks` stay panel-owned. Panel stays lazy in `SettingsActiveConfigPanel` with `Suspense fallback={<SettingsLoading />}` around the existing panel JSX only so the hook is billed to a `NotificationChannelsPanel-*` chunk under the default gzip cap rather than frozen `SettingsPage` / `settings-route` families. Panel imports the hook file directly (not the barrel). Remaining Settings section-local loaders stay out of this row. Issue #789 stays open. | #789 |
| Settings scheduled-task run history GET (`useScheduledTaskRunHistoryQuery`) | `hooks/useScheduledTaskRunHistoryQuery.ts` / `ScheduledTaskRunHistory.tsx` | Imperative `fetchQuery` only (no live `useQuery` observer, no `useInfiniteQuery`, no `useMutation`); exact key `['scheduled-tasks','runs', taskId, limit]` (numeric `limit`, never `String(limit)`); transport `scheduledTasksApi.listRuns(taskId, { limit })` with no AbortSignal; `retry: false`; no Query `refetchInterval`; `refetchOnWindowFocus: false` (overrides app-root default `true`); `networkMode: 'always'`; `staleTime: 0`; request-id owner; React state for `{runs, total, limit, isLoading, error}` (cache is transport-only; no `isRefreshing`, `setRuns`, or mount auto-`load()`). Language is display-only and is not a Query key element; `getParsedApiError` is not passed `language`. There is no mount fetch, poll, or focus refetch. `load(requestedLimit)` runs only from expand / refresh / load more / retry. First expand with empty runs, not loading, and no error requests `{ limit: 10 }`. Load more replaces `items` with `limit: min(500, limit + 10)` (not `useInfiniteQuery` / append). Refresh and retry reuse the last **successful** limit. Empty 200 (`items: []` / `total: 0`) is success EmptyState. HTTP errors keep last-good `runs` / `total` / `limit` and parse `ApiErrorAlert`; initial failure stays empty runs + alert (not EmptyState). `taskId` change exact-cancels+removes the previous live four-element key only, resets display to empty / limit 10 / no error, and does not fetch. `disabled` does not skip an already-requested GET and only disables toggle / refresh / load more. Exact-key silent cancel (`silent: true, revert: false`) + `removeQueries`; same-key refresh cancel+remove then `fetchQuery`; key-changing load more exact-removes the previous live key and the successor key then `fetchQuery`; unmount exact-removes this live key only and must not prefix-cancel `['scheduled-tasks']` or exact-cancel `['scheduled-tasks','list']`, `['scheduler']`, `['plugins']`, `['intelligence']`, `['generation-backend']`, `['local-models']`, `['settings']`, `['config-presets']`, `['investment-framework']`, `['watchlist']`, `['scorecard']`, `['kronos']`, `['data-providers']`, `['outbound-activity']`, `['security-audit']`, `['capabilities']`, `['agent-models']`, `['notifications']`, `['report-version-compare']`, or Home keys; silent `CancelledError` / stale generations never `setError` and never overwrite a newer generation; newest generation wins across unmount, `taskId` change, stale 200 versus newer 500 (keep last-good runs + new error), and stale 500 versus newer 200. Do not share the scheduled-tasks list key or import that hook. The per-row getStatus fan-out is scheduled by `useScheduledTaskLatestRunsQuery`. Create / enable / disable stay panel-owned. The scheduler overlap probe is scheduled by `useScheduledTasksOverlapQuery`. History disclosure (`isOpen`) stays component-owned. Host stays already-lazy in `SystemSecuritySection` (`Suspense fallback={null}`) and the hook is imported only from `ScheduledTaskRunHistory` (not the barrel, not the panel, not the section, not SettingsPage) so it bills to a `ScheduledTasksPanel-*` chunk under the default gzip cap rather than frozen `SettingsPage` / `settings-route` families. Remaining Settings section-local loaders stay out of this row. Issue #789 stays open. | #789 |
| Settings scheduler overlap probe GET (`useScheduledTasksOverlapQuery`) | `hooks/useScheduledTasksOverlapQuery.ts` / `SchedulerSettingsCard.tsx` | Imperative `fetchQuery` only (no live `useQuery` observer, no `useInfiniteQuery`, no `useMutation`); exact two-element key `['scheduled-tasks','overlap']` (params baked into `queryFn`, not key elements); transport `scheduledTasksApi.list({ enabled: true, limit: 1 })` with no AbortSignal; `retry: false`; no Query `refetchInterval`; `refetchOnWindowFocus: false` (overrides app-root default `true`); `networkMode: 'always'`; `staleTime: 0`; request-id owner; Query cache is transport-only (`ScheduledTaskListResponse`); React state owns `hasEnabledVersionedTasks: boolean \| null` (no `isLoading` / `isRefreshing` / parsed error). Language is display-only and is not a key element; `getParsedApiError` is unused. Skip GET while `enabled === false` (`!hasSchedulerSettings`) and exact-cancel+remove plus set `null`; the card `disabled` prop does not skip GET. Mount and parent `statusRefreshToken` change share one `load()` path. Do not expose overlap refresh on the status refresh button, 3s in-run poll, or run-now. Success is `true` if `items.length > 0` or `total > 0`, else `false`; empty 200 is success `false`, not error. HTTP errors fail-soft to `null` with **no last-good** (a later 500 after a 200 must drop `true` to `null` so a stale both-active warning cannot stick). Exact-key silent cancel (`silent: true, revert: false`) + `removeQueries`; same-key refresh cancel+remove then `fetchQuery`; unmount/disable exact-removes this key only and must not prefix-cancel `['scheduled-tasks']` or exact-cancel `['scheduled-tasks','list']`, `['scheduled-tasks','runs',…]`, `['scheduled-tasks','latest-runs']`, `['scheduler']`, `['scheduler','status']`, `['plugins']`, `['settings']`, `['watchlist']`, `['generation-backend']`, `['local-models']`, `['intelligence']`, `['notifications']`, or Home keys; silent `CancelledError` does not set `null` and does not overwrite a newer generation; newest generation wins. Do not share the scheduled-tasks list key, run-history keys, or the scheduler status key, and do not fold this GET into `useSchedulerStatusQuery`. Card imports the hook file directly (not the barrel, not `SystemSecuritySection`, not `SettingsPage`, not `ScheduledTasksPanel`). Card stays lazy in `SystemSecuritySection` (`Suspense fallback={null}`) so card + `useSchedulerStatusQuery` + this hook bill to a `SchedulerSettingsCard-*` chunk under the default gzip cap rather than frozen `SettingsPage` / `settings-route` families. Remaining Settings section-local loaders stay out of this row. Issue #789 stays open. | #789 |
| Settings scheduled-task getStatus fan-out GET (`useScheduledTaskLatestRunsQuery`) | `hooks/useScheduledTaskLatestRunsQuery.ts` / `ScheduledTasksPanel.tsx` | Imperative `fetchQuery` only (no live `useQuery` observer, no `useInfiniteQuery`, no `useMutation`); exact two-element key `['scheduled-tasks','latest-runs']` (task ids baked into `queryFn`, not key elements); transport `Promise.allSettled` of `scheduledTasksApi.getStatus(taskId)` with no AbortSignal (Query `signal` is used only for `throwIfCancelled` after the batch). Not `Promise.all`. `retry: false`; no Query `refetchInterval`; `refetchOnWindowFocus: false` (overrides app-root default `true`); `networkMode: 'always'`; `staleTime: 0`; request-id owner for the fan-out only; Query cache is transport-only (`Record<string, ScheduledTaskRunItem \| null>`); React state owns `latestRuns` (no `isLoading` / `isRefreshing` / parsed error). Language is display-only and is not a key element; `getParsedApiError` is unused on the fan-out path. There is no `enabled` gate, mount auto-`load()`, poll, or focus refetch. Empty `definitions` skip HTTP and set `{}`. Panel `disabled` does not skip fan-out. Success replaces the whole map with fulfilled `taskId → latestRun` entries, including `null` latestRun; rejected GETs are omitted (no last-good per task on fan-out replace); the batch is not failed. Enable/disable follow-up is hook-owned fail-soft `refreshLatestRun(taskId)` that patches one map entry, does not use the fan-out Query key, does not add a second Query key family, and does not bump the fan-out request-id; a 500 keeps the previous entry. Exact-key silent cancel (`silent: true, revert: false`) + `removeQueries` on the fan-out key only; same-key refresh cancel+remove then `fetchQuery`; unmount exact-removes the fan-out key only and must not prefix-cancel `['scheduled-tasks']` or exact-cancel `['scheduled-tasks','list']`, `['scheduled-tasks','overlap']`, `['scheduled-tasks','runs',…]`, `['scheduler']`, `['scheduler','status']`, `['plugins']`, `['settings']`, `['watchlist']`, `['generation-backend']`, `['local-models']`, `['intelligence']`, `['notifications']`, or Home keys; silent `CancelledError` does not write `latestRuns` and does not overwrite a newer generation; newest fan-out generation wins. Do not share the list, overlap, or runs keys, and do not fold this GET into `useSchedulerStatusQuery`. Triggers stay panel-owned: (1) `didInitialFanout` effect after list settles without `loadError`; (2) `loadTasks` after successful list `load()`. Create / enable / disable stay panel-owned. Panel must not call `scheduledTasksApi.getStatus`. Panel imports the hook file directly (not the barrel, not `SystemSecuritySection`, not `SettingsPage`, not `ScheduledTaskRunHistory`, not `SchedulerSettingsCard`). Panel stays already-lazy in `SystemSecuritySection` (`Suspense fallback={null}`) so the hook bills to a `ScheduledTasksPanel-*` chunk under the default gzip cap rather than frozen `SettingsPage` / `settings-route` families. Remaining Settings section-local loaders stay out of this row. Issue #789 stays open. | #789 |
| Settings intelligence items list GET (`useIntelligenceItemsQuery`) | `hooks/useIntelligenceItemsQuery.ts` / `IntelligenceSourcesPanel.tsx` | Imperative `fetchQuery` only (no live `useQuery` observer, no `useInfiniteQuery`, no `useMutation`); exact three-element key `['intelligence','items','list']` (`pageSize: 20` baked into `queryFn`, not a key element; never `String(20)`); transport `intelligenceApi.listItems({ pageSize: 20 })` with no AbortSignal (Query `signal` is used only for `throwIfCancelled` after the GET). Not `listSources`. Not `listTemplates`. No `sourceId` / `scopeType` / `market` / `page` filters. `retry: false`; no Query `refetchInterval`; `refetchOnWindowFocus: false` (overrides app-root default `true`); `networkMode: 'always'`; `staleTime: 0`; request-id owner; Query cache is transport-only (`IntelligenceItemListResponse`); React state owns `items` / `busy` / `actionError`. Language is parse-only on the panel and is not a Query key element; the hook must not call `getParsedApiError`. There is no `enabled` gate, mount auto-`load()`, poll, or focus refetch. Empty catalog of sources does not skip items GET (Load is always shown). Triggers stay panel-owned: user click on Load recent items only. Empty 200 (`items: []`) is success `noItems`. Success non-empty replaces the whole list (not append; not `useInfiniteQuery`); ignore `total` / `page` / `pageSize` for display. HTTP errors keep last-good `items` and parse the shared `InlineAlert`; initial failure stays `items === null` + alert (not `noItems`). Loading: `busy === 'items'` drives the Load button. Exact-key silent cancel (`silent: true, revert: false`) + `removeQueries`; same-key refresh cancel+remove then `fetchQuery`; unmount exact-removes this live key only and must not prefix-cancel `['intelligence']` or exact-cancel `['intelligence','sources','list']` or `['intelligence','templates','list']`, `['generation-backend']`, `['local-models']`, `['settings']`, `['plugins']`, `['config-presets']`, `['investment-framework']`, `['scheduled-tasks']`, `['scheduler']`, `['watchlist']`, `['scorecard']`, `['kronos']`, `['data-providers']`, `['outbound-activity']`, `['security-audit']`, `['capabilities']`, `['agent-models']`, `['notifications']`, or Home keys; silent `CancelledError` does not write `items` / `actionError` and does not overwrite a newer generation; newest generation wins. Do not share the sources or templates keys, and do not fold this GET into `useIntelligenceSourcesQuery`. Mutations stay panel-owned and must not reload items. Panel must not call `intelligenceApi.listItems`. Panel imports the hook file directly (not the barrel, not `SettingsPage`). Panel stays already-lazy in `SettingsPage` with `Suspense fallback={<SettingsLoading />}` around the existing panel JSX only so the hook bills to an `IntelligenceSourcesPanel-*` chunk under the default gzip cap rather than frozen `SettingsPage` / `settings-route` families. Remaining Settings section-local loaders stay out of this row. Issue #789 stays open. | #789 |

### Rollout rules for the next pages

1. Keep transport in `api/*` and UI loading/error presentation on existing surfaces; do not invent a
   parallel error channel or change i18n keys.
2. Prefer route-local query hooks under `hooks/` (or a feature-private subfolder) over page-inline
   `useEffect` triples for list/detail fetch and polling.
3. Match the previous polling cadence and focus/visibility refresh semantics before changing them.
4. Default `retry: false` unless the product contract explicitly wants automatic retries.
5. Migrate one page at a time. Leave Zustand/client state for selection, drafts, and presentation
   until a later dedicated slice.
6. Every host that mounts a Query consumer must wrap a retry-free client. Unit
   tests use `QueryClientProvider` (`retry: false`). Standalone Playwright
   fixtures that mount `Shell` / `NotificationBell` (they do not inherit
   `main.tsx`) must wrap the production `QueryProvider` /
   `createAppQueryClient`. Playground stories rendered through `App` inherit
   the app-root provider; isolated playground unit renders still wrap a test
   client.

Suggested remaining migration order (issue #789): remaining Settings section-local loaders. `StockScreeningPage` interval poll, Chat (#1616), Backtest (#1615), Research Analysis Workbench (#1617), `useWatchlist` GET scheduling, report version-compare, watchlist scores, watchlist groups, watchlist coverage, the Settings Signal Scorecard public preview GET, the Settings data-provider runtime status GET, the Settings Kronos status GET, the Settings outbound-activity GET, the Settings security-audit list GET, the Settings runtime-capabilities inventory GET plus Agent deployment-models GET, the Settings scheduler status GET, the Settings loaded-extensions list GET, the Settings scheduled-tasks list GET, the Settings config-presets list GET, the Settings investment-framework current GET, the Settings Local Models catalog GET, the Settings generation-backend saved GET plus draft preview POST, the Settings intelligence sources list GET plus templates list GET, the Settings notification-channel plugin roster GET, the Settings scheduled-task run history GET, the Settings scheduler overlap probe GET, the Settings scheduled-task getStatus fan-out GET, the Settings intelligence items list GET, and the Settings Local Models runtime GET already landed. Portfolio projection ledger events stay hand-rolled (HOLD). Remaining Settings section-local loaders (`LLMChannelEditor` still raw-calls `getGenerationBackendStatus()` and `getRuntime()`, plus ConfigBackup / preset apply, intelligence mutations) stay ungraduated. Plugins list GET, scheduled-tasks list GET, config-presets list GET, investment-framework current GET, local-models catalog GET, generation-backend status GET/preview, intelligence sources list GET plus templates list GET, the notification-channel plugin roster GET, the scheduled-task run history GET, the Settings scheduler overlap probe GET, the Settings scheduled-task getStatus fan-out GET, the Settings intelligence items list GET, and the Settings Local Models runtime GET have landed. Defer surfaces owned by concurrent open PRs. Issue #789 stays open; graduating this GET does not mean every remaining surface has migrated.

## Change Checklist

Before adding or moving a module:

1. Name the owner by the behavior or contract it provides, not by its first consumer.
2. Check both imports and re-exports; a barrel does not change dependency direction.
3. Put shared types and pure policy below the UI that consumes them.
4. Add or update a focused guard fixture when changing an enforced rule.
5. Remove and lower allowances when debt is retired. New allowances require a concrete owner,
   removal condition, and review of why the dependency cannot be corrected in the same change.
6. Run lint, TypeScript build checking, the affected guard tests, and the production build.

Large files, by themselves, are not architecture violations. Split a module only when the new
boundary has cohesive ownership, reduces meaningful coupling, and can be verified independently.
