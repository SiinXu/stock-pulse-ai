"""API-local application services (HTTP use-case orchestration).

These modules may import ``api.v1.schemas`` / ``api.v1.errors``. Foundation
``src/`` code must not import delivery-boundary DTOs.
"""
